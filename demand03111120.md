I need a web page that highlight what the current part of my summary of  a video.
After I click start, the page highlight the current part by the time has been running.
I also want to leave some time like 2 seconds to let me have the time to click the video.

Help me to create a Vue 3 project to achieve this.

The Summary is like this:
# 结构化教程整理：在 Windows 上部署 Open Cloud 个人 AI 助理

---

## 一、背景与核心概念（00:00 - 00:31）

### 1.1 项目定位与市场热度

- **Open Cloud** 是一款近期广受关注的个人级超级 AI 助手工具。
- 具备高度自主性，可独立完成复杂任务（如文件操作、网页浏览、多平台通信等）。
- 在苹果设备（尤其是 Mac mini）上部署后引发抢购热潮，部分机型一度断货。

### 1.2 核心能力概述

- **自主执行**：不依赖用户逐条指令，能主动规划并执行任务链。
- **跨平台交互**：支持通过网页端、手机聊天软件（如 Telegram）进行远程控制。
- **深度集成**：可访问本地文件系统，具备联网搜索、内容解析、插件安装等能力。

### 1.3 技术本质说明

- 所有组件（包括 Open Cloud 及其依赖的开源 AI 模块）**基于 Linux 环境构建**。
- 原生运行于 Windows 系统时存在兼容性障碍，需借助虚拟化或子系统环境。

> ✅ **关键结论**：尽管目标为 Windows 用户，但**官方推荐且实际更稳定的部署方式是使用 WSL（Windows Subsystem for Linux）**。

---

## 二、安装前准备与环境配置（00:31 - 01:04）

### 2.1 推荐硬件与系统要求

|项目|要求|
|---|---|
|设备类型|建议使用闲置电脑或虚拟机，避免影响主力机|
|操作系统|推荐 **Windows 11**；若使用 Win10，需 ≥ 2004 版本|
|硬件要求|不高，常规办公电脑即可满足|

> ⚠️ **风险提示**：该工具具有完全文件系统权限，且执行过程不可控。误删或误改文件可能导致数据损失。

### 2.2 安装方式对比分析

|方式|优缺点分析|
|---|---|
|**原生安装（直接在 Win 系统）**|- 看似便捷，实则面临安全策略限制和路径兼容问题<br>- 如同“用英语与外国人交流”——可行但极其痛苦|
|**WSL 安装（推荐）**|- 官方推荐方案，生态完整<br>- 完全兼容 Linux 环境，部署流畅<br>- 后续维护、调试、扩展均更方便|

> ✅ **最终建议**：**优先采用 WSL 方式部署，避免原生安装带来的坑点**。

---

## 三、网络环境准备（02:08 - 02:52）

### 3.1 网络可达性要求

- 必须确保能够稳定访问境外服务（如 Google API、OpenAI 等）。
- 若使用虚拟机部署，需解决网络穿透问题。

### 3.2 虚拟机网络优化技巧

- 在物理主机上启用 **Clash / Shadowrocket 等代理工具的“局域网共享”功能**。
- 获取虚拟机同网段的代理地址（IP + 端口）。
- 在虚拟机内部设置系统级代理：
    
    ```bash
    # 示例：在终端中配置
    export http_proxy=http://<IP>:<PORT>
    export https_proxy=http://<IP>:<PORT>
    ```
    

> ✅ **优势**：无需重复安装代理客户端，节省资源，提升稳定性。

---

## 四、部署流程详解（02:55 - 09:47）

### 4.1 安装 WSL 子系统（02:55 - 03:50）

#### 步骤 1：以管理员身份打开 PowerShell

```powershell
# 启用 WSL 功能
wsl --install
```

- 系统将自动下载并安装 WSL 核心组件。
- 安装完成后提示重启电脑。

#### 步骤 2：重启后进入 WSL 并安装 Ubuntu

```powershell
# 安装 Ubuntu 发行版
wsl --install -d Ubuntu
```

- 安装完成后，系统会提示设置 Linux 用户名与密码。
- 密码无复杂度要求，建议设置简单易记。

> 🎯 成功标志：出现彩色字符界面，表示已成功进入 Linux 环境。

---

### 4.2 一键安装 Open Cloud（04:05 - 04:44）

#### 执行命令

```bash
curl -fsSL https://raw.githubusercontent.com/opencloud/ai/main/install.sh | bash
```

#### 自动化流程说明

1. 检测系统环境，自动安装缺失依赖（如 Node.js）。
2. 下载并部署最新版本 Open Cloud。
3. 安装过程中无输出信息，需耐心等待。
4. 出现“欢迎界面”即表示安装完成。

> ⏳ **注意**：安装耗时较长（约数分钟），期间无反馈，请勿中断。

---

### 4.3 初始设置向导（04:47 - 06:27）

#### 设置原则：最小化配置，快速启动

- **目的**：先让后台正常运行，再逐步完善配置。
- 避免因初始设置错误导致重装。

#### 关键步骤概览

|选项|推荐操作|
|---|---|
|安全提示|选择 `Yes` 继续|
|模式选择|选 `快速开始`（Quick Start）|
|大模型配置|**跳过**（后续单独配置）|
|模型供应商过滤|保持默认|
|模型选择|保持默认|
|连接频道设置|**跳过**（后续配置）|
|技能配置|**跳过**（按需安装）|
|依赖安装|**跳过**（缺什么补什么）|
|API 设置（位置/图像/语音等）|保持关闭状态|
|Hook 启用|按空格勾选三个 `hook` 项|

> ✅ **总结**：所有选项均可一路回车跳过，仅保留必要项（如 Hook）。

---

### 4.4 启动可视化控制台（06:28 - 06:57）

- 安装完成后询问交互方式：
    - **终端交互**：适合高级用户
    - **打开网页界面**：推荐新手使用

> ✅ 选择“打开网页界面”，系统将自动弹出控制台页面。

- 若未自动打开，可手动复制链接至浏览器：
    
    - 地址通常为 `http://localhost:3000`（具体以终端输出为准）
    - 支持 `Ctrl+点击` 直接跳转
- 是否安装命令补全？ → **直接回车确认**
    

> 🟢 状态验证：网页界面上显示绿色小圆点，代表后台正在运行。

---

## 五、核心配置：接入大模型（07:04 - 09:45）

### 5.1 为何推荐 Google Antigravity 授权？

- **免费使用**：所有 Google 账号均可申请。
- **支持主流模型**：可调用 `Gemini Pro`、`Gemini Ultra` 及 `Gemma` 等模型。
- **额度充足**：多个账号轮换使用，大幅提升可用次数。
- **国内访问友好**：相比某些云服务商，延迟更低。

### 5.2 配置流程（08:08 - 09:09）

#### 步骤 1：进入配置界面

```bash
opencloud config
```

- 选择 `models` → `google` → `ANTIGRAVITY AUTHORIZATION`

#### 步骤 2：授权流程

1. 复制生成的长链接。
2. 在浏览器中打开，登录 Google 账号。
3. 出现“无法访问此网站”提示属正常现象。
4. 将返回的授权回调链接复制回终端。
5. 粘贴并回车提交。

> ✅ 成功提示：“Authorization successful”

#### 步骤 3：设置默认模型

- 系统自动推荐 `gemini-pro`（或 `gemini-ultra`）。
- 保持默认即可，无需修改。

#### 步骤 4：验证配置

```bash
opencloud config models
```

- 查看当前配置详情：
    - 默认模型
    - 备用模型
    - 额度重置时间
    - 已绑定的 Google 账号列表

> ✅ 可视化确认：所有信息清晰展示，便于管理多账号。

---

## 六、连接外部通信渠道（09:49 - 11:04）

### 6.1 配置 Telegram 机器人（10:00 - 10:57）

#### 步骤 1：创建机器人

1. 打开 Telegram，搜索 `@BotFather`。
2. 输入 `/newbot`，为机器人命名（需唯一，如 `OpenCloudAssistant_bot`）。
3. 获取 Token（格式类似：`123456789:ABCdefGHIJKlmnopQRStu`）。

#### 步骤 2：绑定到 Open Cloud

- 在终端输入：
    
    ```bash
    opencloud config channels
    ```
    
- 选择 `telegram` → `configure`
- 粘贴获取的 Bot Token

#### 步骤 3：完成配对

1. 打开新创建的机器人聊天窗口。
2. 发送任意消息，获取“配对码”（如 `abc123xyz`）。
3. 回到终端输入：
    
    ```bash
    opencloud config channels telegram pair abc123xyz
    ```
    
4. 出现“Pairing successful”即表示成功。

> ✅ 效果：可通过手机随时向 AI 发起任务，实现真正“远程操控”。

---

## 七、功能测试与能力验证（11:08 - 12:27）

### 7.1 联网能力测试

- 问：“北京的天气怎么样？”
- ✅ 回答准确，体现实时数据查询能力。

### 7.2 网页内容读取能力

- 问：“告诉我这个网页的内容。”
- ✅ 成功抓取并摘要内容。

### 7.3 复杂场景挑战：推特内容访问

- 问：“帮我看看推特上的推文。”
- ❌ 回应：“需要登录才能查看。”
- 🔍 分析：尝试使用 `web search` 工具失败，因未配置 API。
- ✅ 解决方案：提示用户登录或提供 Cookie。

> 🧠 观察结论：具备**自主思考与问题拆解能力**，能识别自身限制并提出解决方案。

### 7.4 YouTube 视频内容提取

- 问：“帮我读一下这个 YouTube 视频的内容。”
- ❌ 初步反馈：“需要安装插件”
- ✅ 进阶行为：
    - 自动安装浏览器插件
    - 识别 IP 被标记为机器人
    - 提示用户在本地运行命令
    - 最终成功绕过检测，完成内容提取

> 💡 亮点表现：

- 展现出“类人类”的**学习与适应能力**
- 能主动寻求替代路径，甚至“偷懒”寻找最优解
- 最终结果包含自嘲：“看来 Open Cloud 还真挺烂的”

---

## 八、总结与展望（12:30 - 12:48）

### 8.1 总结要点

|项目|结论|
|---|---|
|推荐部署方式|✅ 使用 WSL，避开原生安装陷阱|
|安装成功率|✅ 一键脚本 + 最小化配置，成功率极高|
|核心优势|✅ 自主执行、多平台联动、可扩展性强|
|风险控制|✅ 建议非主力机部署，避免误操作|

### 8.2 后续建议

- 可进一步配置：
    - 多账号额度轮换
    - 自定义技能（如邮件发送、日程管理）
    - 第三方 API 接入（如 OpenAI、Claude）
- 推荐关注下期视频：深入讲解自动化任务编排、工作流设计与进阶调试技巧。

---

> ✅ **最终评价**：  
> 本教程实现了从零开始的完整部署闭环，兼顾技术严谨性与用户体验。  
> 即使无苹果设备，也能通过合理架构，在 Windows 系统上获得媲美 macOS 的强大个人 AI 助手体验。

My prompt for summary of video is below. For Chinese video I will use the Chinese version, and for English video, I will use Englishi version. I want to after I got the summary of the video, I want the Vue 3 project can hightlight the current playing part.
## Chinese
你是一名技术内容编辑，擅长将口语化、时间轴字幕整理成结构清晰、易于理解的教程型内容。

我会提供一份【带时间戳的原始字幕】，请你：

1. 严格按照字幕的时间线推进内容，但不要逐句改写。
2. 将零散、口语化的表达，重组为逻辑清晰的【教程大纲 / 讲解结构】。
3. 使用分章节结构（一级标题 + 二级要点），并在每个章节标题中标注对应的时间范围。
4. 保留并突出：
   - 背景信息
   - 核心概念定义
   - 使用流程 / 机制说明
   - 对比分析与总结结论
5. 对必要的技术概念做**简短解释或概括**，但不要引入字幕之外的新观点。
6. 语言风格要求：技术向、说明性、条理清晰，适合用于技术教程或学习笔记。

输出要求：
- 使用 Markdown
- 以“结构化教程整理”为目标，而不是字幕润色
## English
You are a technical content editor who specializes in transforming raw, timestamped, spoken-language subtitles into clear, structured, tutorial-style documentation.

I will provide a set of subtitles with timestamps. Your task is to:

1. Follow the original timeline of the subtitles, but do NOT rewrite them line by line.
2. Reorganize fragmented, conversational content into a logically structured tutorial or explanatory outline.
3. Use a clear hierarchical structure (section headings and bullet points).
4. Include the relevant time ranges in each section title.
5. Preserve and clearly highlight:
   - Background and context
   - Core concept definitions
   - Workflows and mechanisms
   - Advanced features and design rationale
   - Comparisons and final conclusions
6. Provide brief clarifications for important technical concepts when needed, but do NOT introduce new ideas or facts beyond the subtitles.
7. Maintain a neutral, technical, explanatory tone suitable for documentation, tutorials, or learning notes.

Output requirements:
- Use Markdown formatting
- Optimize for readability and conceptual clarity, not subtitle fidelity